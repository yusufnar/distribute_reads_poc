default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: postgres
  password: secret
  database: appdb

development:
  primary:
    <<: *default
    host: pg-primary
  primary_replica:
    <<: *default
    host: pg-replica1
    replica: true
